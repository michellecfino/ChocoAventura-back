spring:
  application:
    name: chocoaventura

  datasource:
    url: jdbc:postgresql://localhost:5432/chocoaventura_db
    username: choco
    password: lala
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true